
<div class="jumbotron">
  <h1 class="display-4"><%= @groups.groupname %> - <%= @groups.grouptype %></h1>
  <p class="lead">This is a simple attendance tracking application.</p>
  <hr class="my-4">
  <p>It uses utility classes for typography and spacing to space content out within the larger container.</p>
  <p class="lead">
    <form class="" action="/students/generate_names" method="get">
      <input type="hidden" name="groupid" value="<%= @groups.groupid %>">

      <input type="submit" class="btn btn-primary btn-lg" role="button" value="Fill Names">
    </form>
  </p>
</div>

  <table id="myTable" class="table">
    <thead class="thead-dark">
      <tr>
        <th scope="col">Student ID</th>
        <th scope="col">First Name</th>
        <th scope="col">Last Name</th>
        <th scope="col">Group ID</th>
      </tr>
    </thead>
    <tbody>
      <% @students.each do |student| %>
      <tr>
        <td><a href="/students/<%= student.studentid %>"><%= student.studentid %></a></td>
        <td><a href="/students/<%= student.studentid %>"><%= student.firstname %></a></td>
        <td><a href="/students/<%= student.studentid %>"><%= student.lastname %></a></td>
        <td><a href="/students/<%= student.studentid %>"><%= student.groupid %></a></td>
      </tr>
      <% end %>
    </tbody>
  </table>
